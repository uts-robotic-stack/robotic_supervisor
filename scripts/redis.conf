# Allow connections from all interfaces (adjust as needed)
bind 0.0.0.0

# Enable or disable protected mode (not recommended for production without authentication)
protected-mode no

# Set the port Redis should listen on
port 6379

# Specify the directory for persistent storage
dir /data

# Enable persistent storage (optional, append-only file)
appendonly yes
